export function MensalTab() {
  return (
    <section className="space-y-6">
      <div className="card p-6 text-sm text-[var(--muted)]">Aba Mensal — em construção (Fase 4).</div>
    </section>
  );
}
